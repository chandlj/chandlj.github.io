const style = (theme) => ({
  contactBackground: {
    height: '35vh',
    backgroundColor: theme.palette.primary.main,
    display: 'flex',
    justifyContent: 'center',
    paddingTop: 40,
  },
});

export default style;
