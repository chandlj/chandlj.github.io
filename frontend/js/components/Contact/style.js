const style = (theme) => ({
  contactBackground: {
    width: '100%',
    height: '35vh',
    maxHeight: '100vh',
    backgroundColor: theme.palette.primary.main,
    display: 'flex',
    justifyContent: 'center',
    paddingTop: 40,
  },
});

export default style;
